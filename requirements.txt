# CPU-only PyTorch from the official index keeps the image small and fast.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110
uvicorn[standard]>=0.29
python-chess>=1.999
numpy>=1.26
requests>=2.31
# PyTorch CPU wheels for Python 3.14+ require torch >= 2.9.0.
torch==2.12.1+cpu